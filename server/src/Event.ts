import { Server } from 'socket.io';

export default class Event {
    private io: 

    public static async initSocket() {
        
    }

    public static async register(socketId: string) {

    }
}